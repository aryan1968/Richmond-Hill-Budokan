# DEPRECATED — the site now deploys via Vercel (connected directly to this
# GitHub repo), not GitHub Pages. Vercel handles builds/deploys automatically
# on every push, so this workflow no longer runs on push. Kept only for
# manual/historical reference; safe to delete this whole file whenever you
# like (Settings → Pages can also be turned back to "None" on GitHub).
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — this project now deploys via Vercel."
